---
import { NAV_ITEMS, TABBED_CONTENT_ID, PHONE_NUMBER } from '../lib/constants';
---
<header id="main-nav" class="fixed top-0 left-0 right-0 z-40 transition-all duration-200">
  <div class="max-w-6xl mx-auto px-6 lg:px-8">
    <nav class="flex items-center justify-between h-16 lg:h-[4.25rem]" aria-label="Main navigation">
      <a href="#" class="font-semibold text-foreground text-[0.9375rem] tracking-tight">BMP: Build. Manage. Perform.</a>
      <button
        type="button"
        id="nav-menu-toggle"
        class="lg:hidden p-2.5 rounded-md text-foreground hover:bg-steel-light focus:outline-none focus:ring-1 focus:ring-primary focus:ring-offset-2"
        aria-expanded="false"
        aria-controls="nav-menu"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
      <div id="nav-menu" class="hidden lg:flex items-center gap-10">
        {NAV_ITEMS.map((item) => (
          <a
            href={`#${TABBED_CONTENT_ID}`}
            class="nav-link text-steel hover:text-foreground text-[0.8125rem] font-medium tracking-wide uppercase"
            data-tab={item.id}
          >
            {item.label}
          </a>
        ))}
        <a
          href={`tel:${PHONE_NUMBER}`}
          class="inline-flex items-center px-4 py-2.5 bg-primary text-on-primary text-[0.8125rem] font-medium tracking-wide rounded-md hover:opacity-95 transition-opacity focus:outline-none focus:ring-1 focus:ring-offset-2 focus:ring-primary"
        >
          {PHONE_NUMBER}
        </a>
      </div>
    </nav>
  </div>
  <div id="nav-menu-dropdown" class="hidden lg:hidden absolute top-full left-0 right-0 bg-background border-b border-divider">
    <div class="max-w-6xl mx-auto px-6 py-5 flex flex-col gap-1">
      {NAV_ITEMS.map((item) => (
        <a
          href={`#${TABBED_CONTENT_ID}`}
          class="nav-link py-2.5 text-steel hover:text-foreground text-sm font-medium"
          data-tab={item.id}
        >
          {item.label}
        </a>
      ))}
      <a
        href={`tel:${PHONE_NUMBER}`}
        class="inline-flex items-center justify-center py-3 mt-2 bg-primary text-on-primary text-sm font-medium rounded-md"
      >
        {PHONE_NUMBER}
      </a>
    </div>
  </div>
</header>

<script define:vars={{ TABBED_CONTENT_ID }}>
  const nav = document.getElementById('main-nav');
  const toggle = document.getElementById('nav-menu-toggle');
  const dropdown = document.getElementById('nav-menu-dropdown');

  function updateNavScroll() {
    if (!nav) return;
    if (window.scrollY > 16) {
      nav.classList.add('bg-background/90', 'backdrop-blur-sm', 'border-b', 'border-divider');
    } else {
      nav.classList.remove('bg-background/90', 'backdrop-blur-sm', 'border-b', 'border-divider');
    }
  }

  window.addEventListener('scroll', updateNavScroll);
  updateNavScroll();

  toggle?.addEventListener('click', () => {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    dropdown?.classList.toggle('hidden', expanded);
  });

  document.querySelectorAll('.nav-link').forEach((link) => {
    link.addEventListener('click', (e) => {
      const tabId = link.getAttribute('data-tab');
      if (tabId) {
        const event = new CustomEvent('nav-tab-change', { detail: { tabId } });
        window.dispatchEvent(event);
      }
      dropdown?.classList.add('hidden');
      toggle?.setAttribute('aria-expanded', 'false');
    });
  });
</script>
