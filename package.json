{
  "name": "bmp-landing",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "serve dist -s",
    "build": "astro build",
    "preview": "astro preview",
    "prepare": "husky || true",
    "secret:scan": "secretlint --maskSecrets --secretlintignore .gitignore \"**/*\""
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "serve": "^14.2.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^11.0.0",
    "autoprefixer": "^10.6.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "secretlint": "^11.0.0"
  },
  "lint-staged": {
    "*": [
      "secretlint --maskSecrets --secretlintignore .gitignore"
    ]
  }
}
